#version 330

layout(location = 2) in vec3 in_pos;
//layout(location = 3) in vec3 in_vel;
out vec3 out_pos;
out vec3 out_vel;

void main(void)
{
    vec3 gravity = vec3(0,-.1,0);
    out_pos = in_pos+(gravity);
    out_vel = vec3(200, 100, 200);
}