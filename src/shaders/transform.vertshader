#version 330

layout(location = 0) in vec3 in_pos;
layout(location = 2) in vec3 in_vel;
out vec3 out_pos;
out vec3 out_vel;

void main(void)
{
    vec3 gravity = vec3(0,-.5,0);
    out_pos = in_pos+gravity+(in_vel/10);
    out_vel = in_vel / 1.001;
}